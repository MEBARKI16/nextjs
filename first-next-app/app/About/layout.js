

export const metadata = {
    title: 'hello',
    description: 'hello2',
};

export default function layout({ children }) {
    return (
       <div>
        <h1>hello</h1>
         {children}
       </div>
          
        
    );
}
